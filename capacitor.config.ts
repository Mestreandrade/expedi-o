import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.expediocontroledeestoque',
  appName: 'Expedição - Controle de estoque',
  webDir: 'dist'
};

export default config;
